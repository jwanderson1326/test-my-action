name: Say Hello

on:
  push:
    branches:
      - main

jobs:
  hello:
    name: Hello
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello to Me
        uses: jwanderson1326/my-second-action@v1
        with:
          who-to-greet: "JUSTIN"
